- name: "Gnome Tasks"
  block:
    - import_tasks: "gnome/config/desktop.yaml"

    - import_tasks: "shared/users/nautilus.yaml"
  tags: ["gnome"]
  vars:
    profileUsername: "{{ userUsername }}"
    profileFullname: "{{ userFullname }}"
    profile_image_folder: "{{ userImageFolder }}"
