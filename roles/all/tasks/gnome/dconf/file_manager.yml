- name: 'Gnome > Dconf > File Manager: Dirs sorted with files'
  become: true
  become_user: '{{ real_username }}'
  community.general.dconf:
    key: '/org/gtk/settings/file-chooser/sort-directories-first'
    state: present
    value: "false"
  tags: ['dconf', 'nauilus', 'gnome']

- name: 'Gnome > Dconf > File Manager: Hide hidden files'
  become: true
  become_user: '{{ real_username }}'
  community.general.dconf:
    key: '/org/gnome/nautilus/preferences/show-hidden-files'
    state: present
    value: "false"
  tags: ['dconf', 'nauilus', 'gnome']

- name: 'Gnome > Dconf > File Manager: Maximise window'
  become: true
  become_user: '{{ real_username }}'
  community.general.dconf:
    key: '/org/gnome/nautilus/window-state/maximized'
    state: present
    value: "true"
  tags: ['dconf', 'nauilus', 'gnome']

- name: 'Gnome > Dconf > File Manager: Set icon view'
  become: true
  become_user: '{{ real_username }}'
  community.general.dconf:
    key: '/org/gnome/nautilus/preferences/default-folder-viewer'
    state: present
    value: "'icon-view'"
  tags: ['dconf', 'nauilus', 'gnome']

- name: 'Gnome > Dconf > File Manager: Set single-click to open'
  become: true
  become_user: '{{ real_username }}'
  community.general.dconf:
    key: '/org/gnome/nautilus/preferences/click-policy'
    state: present
    value: "'single'"
  tags: ['dconf', 'nauilus', 'gnome']

- name: 'Gnome > Dconf > File Manager: Show create link'
  become: true
  become_user: '{{ real_username }}'
  community.general.dconf:
    key: '/org/gnome/nautilus/preferences/show-create-link'
    state: present
    value: "true"
  tags: ['dconf', 'nauilus', 'gnome']

- name: 'Gnome > Dconf > File Manager: Sidebar enabled'
  become: true
  become_user: '{{ real_username }}'
  community.general.dconf:
    key: '/org/gnome/nautilus/window-state/start-with-sidebar'
    state: present
    value: "true"
  tags: ['dconf', 'nauilus', 'gnome']

- name: 'Gnome > Dconf > File Manager: Sidebar width'
  become: true
  become_user: '{{ real_username }}'
  community.general.dconf:
    key: '/org/gnome/nautilus/window-state/sidebar-width'
    state: present
    value: "uint32 250"
  tags: ['dconf', 'nauilus', 'gnome']

- name: 'Gnome > Dconf > File Manager: Zoom level'
  become: true
  become_user: '{{ real_username }}'
  community.general.dconf:
    key: '/org/gnome/nautilus/icon-view/default-zoom-level'
    state: present
    value: "'larger'"
  tags: ['dconf', 'nauilus', 'gnome']
  when: distro == 'ubuntu' and (distro_major_version | int) <= 22

- name: 'Gnome > Dconf > File Manager: Zoom level'
  become: true
  become_user: '{{ real_username }}'
  community.general.dconf:
    key: '/org/gnome/nautilus/icon-view/default-zoom-level'
    state: present
    value: "'medium'"
  tags: ['dconf', 'nauilus', 'gnome']
  when: distro == 'fedora' or distro == 'ubuntu' and (distro_major_version | int) == 23