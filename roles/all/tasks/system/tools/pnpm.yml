- name: 'System > Tools > Install PNPM'
  become: true
  become_user: '{{ real_username }}'
  ansible.builtin.shell: >
    /bin/bash -c "source ~/.nvm/nvm.sh && npm install pnpm"
  tags: ['system', 'dev']
