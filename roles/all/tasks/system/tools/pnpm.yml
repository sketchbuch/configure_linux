- name: 'System > Tools > Install PNPM (globally)'
  become: true
  become_user: '{{ real_username }}'
  ansible.builtin.shell: >
    /bin/bash -c "source ~/.nvm/nvm.sh && npm install --global pnpm"
  tags: ['system', 'dev']
