- name: 'System > Tools > Install Yarn'
  become: true
  become_user: '{{ real_username }}'
  ansible.builtin.shell: >
    /bin/bash -c "source ~/.nvm/nvm.sh && npm install --global yarn"
  tags: ['system', 'dev']
