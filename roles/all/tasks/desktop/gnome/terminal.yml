- name: 'Gnome > Terminal: Show password indicator'
  become: true
  ansible.builtin.lineinfile:
    path: '/etc/sudoers.tmp'
    insertafter: '^Defaults        env_reset'
    line: 'Defaults        pwfeedback'
  tags: ['system', 'terminal', 'files']