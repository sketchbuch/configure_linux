- name: 'Users > {{real_username_pretty}} > Bash: Copy dot files'
  copy:
    src: 'users/{{real_username}}/{{ item.src }}'
    dest: '/home/{{real_username}}/{{ item.dest }}'
    owner: "'{{real_username}}'"
    group: "'{{real_username}}'"
    mode: 0600
  tags: ['users', 'terminal']
  with_items:
    - { src: 'users/{{real_username}}/bash/bashrc', dest: '.bashrc' }