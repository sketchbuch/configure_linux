- name: 'Users > {{real_username | capitalize}} > Set profile image'
  copy:
    src: 'users/{{real_username}}/profile'
    dest: '/var/lib/AccountsService/icons/{{real_username}}'
    owner: 'root'
    group: 'root'
    mode: 0644
  tags: ['users','profile']