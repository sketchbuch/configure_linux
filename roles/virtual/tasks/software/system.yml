- name: 'Software > System > Install Dconf Editor'
  community.general.flatpak:
    name: ca.desrt.dconf-editor
    state: present
    method: system
  tags: ['system', 'dconf-editor', 'flatpak']

- name: 'Software > System > Install Extension Manager'
  community.general.flatpak:
    name: com.mattjakeman.ExtensionManager
    state: present
    method: system
  tags: ['system', 'extension-manager', 'flatpak']

- name: 'Software > System > Install Gnome Extensions'
  community.general.flatpak:
    name: org.gnome.Extensions
    state: present
    method: system
  tags: ['system', 'gnome-extensions', 'flatpak']

- name: 'Software > System > Install Htop'
  package:
    name:
      - htop
  tags: ['system', 'htop', 'package']

- name: 'Software > System > Install Powertop'
  package:
    name:
      - powertop
  tags: ['system', 'powertop', 'package']

- name: 'Software > System > Install Neofetch'
  package:
    name:
      - neofetch
  tags: ['system', 'neofetch', 'package']