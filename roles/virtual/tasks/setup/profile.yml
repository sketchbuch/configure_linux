- name: 'Setup > Profile > Add user to vboxfs group'
  ansible.builtin.user:
    name: '{{ real_username }}'
    groups: 'vboxsf'
    append: true
  when: is_virtual and ansible_virtualization_type == 'virtualbox'