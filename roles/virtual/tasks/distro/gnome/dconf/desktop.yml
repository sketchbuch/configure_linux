- name: "Gnome > Dconf > Hot Corners: Disable in virtual environments"
  become: true
  become_user: '{{real_username}}'
  community.general.dconf:
    key: "/org/gnome/desktop/interface/enable-hot-corners"
    state: present
    value: "false"
  tags: ['dconf', 'desktop', 'gnome']