---

- name: freefilesync | Copy sync configs
  become: true
  become_user: "{{ user.username }}"
  ansible.builtin.copy:
    dest: "/home/stephen/Apps/FreeFileSync"
    group: "{{ user.username }}"
    mode: preserve
    owner: "{{ user.username }}"
    src: "app_data/freefilesync/"
