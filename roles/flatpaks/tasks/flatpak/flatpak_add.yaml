---

- name: "flatpak_add | Install {{ group_name }}"
  community.general.flatpak:
    method: user
    name: "{{ app_name }}"
    remote: "{{ remote }}"
    state: present
  loop: "{{ apps }}"
  loop_control:
    loop_var: app_name
