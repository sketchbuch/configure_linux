---

- name: Install fonts
  ansible.builtin.include_tasks: "{{ font_file }}"
  with_fileglob:
    - ./fonts/*.yaml
  loop_control:
    loop_var: font_file

- name: Set system fonts
  ansible.builtin.include_tasks: ./system/fonts.yaml
