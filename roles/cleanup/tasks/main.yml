- block:
  - import_tasks: 'software/unwanted.yml'

  - import_tasks: 'system/apt.yml'
    when: is_ubuntu and is_gnome
  
  - import_tasks: 'system/fonts.yml'
  
  rescue:
    - set_fact: cleanup_task_failed=true