- name: 'System > Tools > VSCE: Install for all node versions'
  become: true
  become_user: '{{ profile_username }}'
  ansible.builtin.shell: >
    /bin/bash -c "source ~/.nvm/nvm.sh && nvm use {{ nodeVersion }} && npm install -g vsce"
  loop: '{{ nodeVersions }}'
  loop_control:
    loop_var: nodeVersion
  tags: ['system', 'dev']