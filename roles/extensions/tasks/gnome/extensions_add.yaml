---

- name: extensions_add | Install {{ group_name }}"
  become: true
  become_user: "{{ user_username }}"
  loop: "{{ exts }}"
  loop_control:
    loop_var: ext
  ansible.builtin.include_tasks: ./extensions_install2.yaml
