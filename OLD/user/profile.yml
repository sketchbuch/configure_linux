- name: 'Add user to vboxfs group'
  become: true
  become_user: '{{real_username}}'
  ansible.builtin.user:
    name: "{{ real_username }}"
    groups: "vboxfs"
    append: true
  when: is_virtual && ansible_virtualization_type == 'virtualbox'