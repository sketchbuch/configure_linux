- hosts: localhost
  connection: local
  become: true
  
  tasks:
  
  - name: Print all available facts
    ansible.builtin.debug:
      var: ansible_facts
  - name: Install system tools
    package:
      name:
        - htop
        - neofetch
        - plocate
        - powertop

 # - name: install fonts
 #   package:
 #     name:
 #       - fonts-cantarell