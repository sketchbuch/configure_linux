- hosts: localhost
  connection: local
  become: false
  
  tasks:
  - name: install system tools
    package:
      name:
        - htop
        - neofetch
        - plocate
        - powertop

 # - name: install fonts
 #   package:
 #     name:
 #       - fonts-cantarell