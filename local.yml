- hosts: localhost
  connection: local
  become: true
  
  tasks:
  - name: install system tools
    package: 
      name: 
        - htop
        - neofetch
        - plocate
        - powertop

  - name: install fonts
    package: 
      name: 
        - fonts-cantarell

  - name: install dev tools
    package: 
      name: 
        - create-next-app
        - create-react-app
        - ovsx
        - vsce